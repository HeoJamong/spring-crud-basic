spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update # 공부해보기
    show-sql: true
    properties:
      hibernate:
        format_sql: true